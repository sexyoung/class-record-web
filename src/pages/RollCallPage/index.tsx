import { Header } from "components/Header";
import { useEffect } from "react";

export const RollCallPage = () => {
  useEffect(() => {
  });

  return (
    <div>
      <Header />
      點名頁面

    </div>
  );
};
